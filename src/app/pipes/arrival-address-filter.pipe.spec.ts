import { ArrivalAddressFilterPipe } from './arrival-address-filter.pipe';

describe('ArrivalAddressFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrivalAddressFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
