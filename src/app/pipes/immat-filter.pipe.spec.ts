import { ImmatFilterPipe } from './immat-filter.pipe';

describe('ImmatFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImmatFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
