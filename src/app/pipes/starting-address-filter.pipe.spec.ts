import { StartingAddressFilterPipe } from './starting-address-filter.pipe';

describe('StartingAddressFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StartingAddressFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
